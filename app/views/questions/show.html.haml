%h2= @question.title
%h3= @question.body

=if user_signed_in? and current_user == @question.user
	.floatl= link_to 'Delete', question_path(@question), method: :delete

-@question.answers.each do |a|
	%hr= a.body

%p= link_to 'Answer', new_question_answer_path(@question) if user_signed_in?